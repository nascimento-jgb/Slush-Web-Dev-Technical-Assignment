{
  "name": "to_do_app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm iinstall) || (cd client && npm install)",
    "server": "node ./server/serve.js",
    "client": "cd client && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4",
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "@types/pg": "^8.10.1"
  }
}
